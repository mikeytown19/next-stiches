import { styled } from '../../theme/stiches.config';
import { flexVariants, paddingVariants, marginVariants } from '../../theme/variants';

const Box = styled('div', {

}, flexVariants, paddingVariants, marginVariants);

export default Box;
